cmake_minimum_required(VERSION 2.8)
project( Calibration )
include_directories(${PROJECT_SOURCE_DIR}/include)
find_package( OpenCV REQUIRED )
add_library(lib calibration.cpp)
target_link_libraries( lib ${OpenCV_LIBS} )